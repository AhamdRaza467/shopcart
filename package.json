{
  "name": "shopcart-root",
  "private": true,
  "scripts": {
    "vercel-build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build && mv frontend/dist public"
  }
}
